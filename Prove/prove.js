// @ts-check
import { updateMyProperties, thingsWithId, preCalc, sum, sort } from "../lib/Minos.js";

const web = updateMyProperties(ting);

const { bestill, navn, epost,adresse,op,oppo  } = thingsWithId();



bestill.onclick = () => {
   bestilli