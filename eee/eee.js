
const alleBrukere = {};

const leggMegTil = (id, navn, etternavn, alder, adresse, tlf) => {
    
    alleBrukere[id] = {
            navn: navn,
            etternavn: etternavn,
            alder: alder,  
            adresse: adresse,
            tlf: tlf,
        }
    }

leggMegTil("01", "Rekan", "Lutfi", 18, "Afghanistan", 6969696969);
leggMegTil("02", "Nathaniel", "Stensen", 20, "Rikmannsområdet", "penger");
leggMegTil("03", "Joe", "Jackson", 23, "Westside", 12345678910);

// Liste over alle idene som finnes i alleBrukere
// ["01", "02", "03"]
const AlleIds = Object.keys(alleBrukere);

// Tom liste som skal ha navn
const bareNavn = []

// array.push(obj)

// Går gjennom AlleIds 0..lengden av AlleIds
for(let i=0; i<AlleIds.length; ++i){
    // id er id nr i
    const id = AlleIds[i];
    // bruker nr id nr i
    const bruker = alleBrukere[id];
    bareNavn = navn
}

console.log(bareNavn);

let a = [1,2] 
let b = [3,4] 
a-b
let avstand = dist(a,b)
avstand = Math.sqrt(ax*bx + ay*by)

const x = a.split(" ")
const p = x.pop()
x.unshift("Andersen")  
const t = x.join(" ")

const x = a.split(" ")
const p = x.pop()
navnebiter.pop ()
x.unshift(" ")
const t = x.join(" ")
return navnebiter

const s = (" ")
      s += a
return s

for(let s<0;s<0;s++)
console.log ()

const elevTabell = [ "Ole","Per","Kari","Lise","Lars","Oda" ];
let s; 

for (let s=0; i < arr.length; s++) {
   const verdi = arr[s];
}

for (const elev of elevTabell) {
  console.log(elev);
}
elev  <=> elevTabell[s]


const sum = 0
v = a[i]
 sum =+ v
if (s >= b){
return (i) 
}
return a.length


const elevTabell = [ "Ole","Per","Kari","Lise","Lars","Oda" ];
let s; 

for (let s=0; i < arr.length; i++) {
   const verdi = arr[s];
}

for (const elev of elevTabell) {
  console.log(elev);
}
elev = elevTabell[s]

const element = wrapMe(((span)${variabelnavn}(/span)\))
console.log(element)

$("${variabelnavn}")
    .contents()
    .filter(
        function(){
            return this.nodeType != 1; 
        })
    .wrap("<span/>");



const liste = lagListe[ "anne","ola","per","kari","beate","bjørn" ];
console.log(liste)
return liste 

const liste = lagListe[ "anne","ola","per","kari","beate","bjørn" ];
console.log(liste)
return liste 










const alleBrukere = {};

const leggMegTil = (id, navn, etternavn, alder, adresse, tlf) => {
    
    alleBrukere[id] = {
            navn: navn,
            etternavn: etternavn,
            alder: alder,  
            adresse: adresse,
            tlf: tlf,
        }
    }

const AlleIds = Object.keys(alleBrukere);

const bareNavn = []

for(let i=0; i<AlleIds.length; ++i){
    // id er id nr i
    const id = AlleIds[i];

 

}

r

const s = (" ")
      s += a
return s

for(let s<0;s<0;s++)
console.log ()

const liste = lagListe[ "anne","ola","per","kari","beate","bjørn" ];
for(let s<0;s<0;s++)
console.log(liste)
return liste 






const sum = 0
let a = ()
const s =+ v

if (s >= limit (b)){
false = ()
}
true return i;
a.length


  la v være elementet på plass i // a[i]
  s øker med v
  dersom s er større en grensa i b
    gi tilbake i
slutt på løkka
gi tilbake lengden på lista // a.length









(span) + (span)${xxx}(/span)\ + (/span)

`(span) + `(span)${variabelnavn}`(/span)\ + `(/span)`

'(`(span)${a}(/span)\`)' + ${variabelnavn} + '(/`(span)${a}(/span)\`)'



if (s >= limit (b)){
    return (i) 
    }
    if (s >= limit (b)){
        return (i) 
        }